{
  "name": "studiai-audio-extractor",
  "version": "2.0.0",
  "private": true,
  "description": "Cloud Run worker that runs the full lesson AI pipeline (download → ffmpeg → Azure Speech → Azure OpenAI → Firebase Storage upload + Firestore update).",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "firebase-admin": "^13.6.0"
  }
}
