// Invalid test case - Path with parent directory reference
// This should fail semantic validation

agent "unsafe-agent" {
  intent "This agent has unsafe path references";

  capability write ["filesystem"];
  
  // This path is invalid - contains parent directory reference
  emit "../../etc/passwd";
}
